<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.agung.sedated">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_hope"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_leaf"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sedated">
        <activity android:name=".DataTanaman"></activity>
        <activity android:name=".InfoActivity" />
        <activity android:name=".TanamanActivity" />
        <activity android:name=".DataPenyakit" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>